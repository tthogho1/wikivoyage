# pinned dependencies (installed in .venv)
requests==2.34.2

# optional: nicer wikitext parsing for the preprocessor
mwparserfromhell>=0.6.6

# embedding + Zilliz Cloud
pymilvus[model]>=2.4.0
torch>=2.0
python-dotenv>=1.0

# Chatbot API
fastapi>=0.110
uvicorn[standard]>=0.29
openai>=1.0
